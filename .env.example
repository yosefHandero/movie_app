# Client (Expo) — safe to prefix with EXPO_PUBLIC_
EXPO_PUBLIC_MOVIE_API_KEY=
# Leave empty on Vercel (same-origin). Use http://localhost:3000 for vercel dev.
EXPO_PUBLIC_API_URL=

# Server only — set in Vercel Environment Variables, never EXPO_PUBLIC_
POSTGRES_URL=
